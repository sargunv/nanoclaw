# Base tools baked into the container image.
# The agent can install additional tools via its own config.toml.
[tools]
ffmpeg = "latest"
gh = "latest"
"github:schpet/linear-cli" = "latest"
java = "lts"
node = "lts"
python = "latest"
